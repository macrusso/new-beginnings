{
  "name": "aws-sam-lambda-typescript-hexagonal-architecture",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1348.0",
    "dynamodb-toolbox": "^0.7.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/aws-sdk": "^2.7.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.11",
    "@types/uuid": "^9.0.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.3"
  },
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "build": "tsc && cp package.json dist"
  }
}
